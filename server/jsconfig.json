{
  "compilerOptions": {
    // Set base dir to the project root (where package.json lives)
    "baseUrl": ".",
    // Define custom path aliases = clean imports:
    "paths": {
      "@/*": [
        "./*" // This now correctly maps '@/' to everything in the fullproject directory
      ]
    }
  }
}