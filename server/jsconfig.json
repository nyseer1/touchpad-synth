{
    "compilerOptions": {
        // base dir set to  project root (contains project.json)
        "baseUrl": ".",
        // Defined custom path aliases = clean imports:
        "paths": {
            "@/*": [
                "./*"
            ] // Maps '@/' to everything inside 'src'
        },
    }
}